create table c_name_value_t(
    id int(4) primary key auto_increment,
    name varchar(16),
    value varchar(32)
);
create table test(
    id int(4) primary key auto_increment,
    name varchar(16),
    value varchar(32)
);